<!--<mvc:View controllerName="com.danielwehner.invoicekuga.controller.customer.CustomerDetail" height="100%"
	xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc" xmlns:table="sap.ui.table">
    <Panel>
        <content>
            <table:Table
				rows="{/contacts}"
				title="Contacts"
				selectionMode="MultiToggle"
				visibleRowCount="7">
				<table:columns>
					<table:Column width="11rem">
						<Label text="Contact Type"/>
						<table:template>
							<Text text="{type}"/>
						</table:template>
					</table:Column>
                    <table:Column width="11rem">
						<Label text="Contact"/>
						<table:template>
							<Text text="{data}"/>
						</table:template>
					</table:Column>
                    <table:Column width="11rem">
						<Label text="Comments"/>
						<table:template>
							<Text text="{comments}"/>
						</table:template>
					</table:Column>
				</table:columns>
			</table:Table>
        </content>
    </Panel>
</mvc:View>-->
